<core:FragmentDefinition xmlns="sap.m"
    xmlns:grid="sap.ui.layout.cssgrid"
    xmlns:core="sap.ui.core"
    xmlns:unified="sap.ui.unified"
    xmlns:u="sap.ui.unified">
    <VBox class="vCardBox small sapUiMediumMarginEnd sapUiSmallMargin" busyIndicatorDelay="0" busy="{/busy/plan}">
    <!-- appointmentSelect="handleAppointmentSelect"
			rowHeaderPress="handleRowHeaderPress" -->
		<PlanningCalendar
			id="PC1"
			startDate="{path: '/startDate'}"
			rows="{path: '/people'}"
			appointmentsVisualization="Filled"
			showEmptyIntervalHeaders="false"
            stickyHeader="false"
			showWeekNumbers="true"
            builtInViews="Hour"
            fullDay="false"
            startHour="1"
            endHour="24"
            startDateChange=".hanleDateChangeWorkState"
            >
			<rows>
				<PlanningCalendarRow
					icon="{pic}"
					title="{name}"
					text="{role}"
					appointments="{path : 'appointments', templateShareable: false}"
					intervalHeaders="{path: 'headers', templateShareable: false}">
					<customData>
						<core:CustomData key="emp-name" value="{name}" writeToDom="true"/>
					</customData>
					<appointments>
						<unified:CalendarAppointment
							startDate="{start}"
							endDate="{end}"
							icon="{pic}"
							title="{title}"
							text="{info}"
							type="{type}"
							tentative="{tentative}">
						</unified:CalendarAppointment>
					</appointments>
					<intervalHeaders>
						<unified:CalendarAppointment
							startDate="{start}"
							endDate="{end}"
							icon="{pic}"
							title="{title}"
							type="{type}">
						</unified:CalendarAppointment>
					</intervalHeaders>
				</PlanningCalendarRow>
			</rows>
		</PlanningCalendar>
	</VBox>
</core:FragmentDefinition>